import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({

}));

const CarouselDrawer = ({movie}) => {
	const classes = useStyles();

	return (
		<div>Drawer Open!</div>
	);
}

export default CarouselDrawer;
